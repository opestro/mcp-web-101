<%- include('layout', { body: `
<div class="max-w-4xl mx-auto">
    <!-- Back to Home -->
    <a href="/" class="inline-flex items-center text-blue-500 hover:text-blue-600 mb-8">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Back to Home
    </a>

    <h1 class="text-4xl font-bold text-gray-800 mb-8">Installation Guide ⚙️</h1>

    <div class="space-y-12">
        <div class="bg-white shadow-md rounded-lg p-6 relative">
            <div class="step-number absolute -left-4 top-6">1</div>
            <h2 class="text-2xl font-semibold text-gray-700 mb-4">Prerequisites 📋</h2>
            <ul class="space-y-3">
                <li class="flex items-center text-gray-600">
                    <span class="text-green-500 mr-2">✓</span>
                    Node.js (version 14 or higher)
                </li>
                <li class="flex items-center text-gray-600">
                    <span class="text-green-500 mr-2">✓</span>
                    npm or yarn package manager
                </li>
                <li class="flex items-center text-gray-600">
                    <span class="text-green-500 mr-2">✓</span>
                    Basic knowledge of JavaScript/Node.js
                </li>
            </ul>
        </div>

        <div class="bg-white shadow-md rounded-lg p-6 relative">
            <div class="step-number absolute -left-4 top-6">2</div>
            <h2 class="text-2xl font-semibold text-gray-700 mb-4">Create a New Project 📁</h2>
            <div class="bg-gray-100 p-4 rounded-md mb-4 relative group">
                <button class="absolute right-2 top-2 text-gray-400 hover:text-blue-500 opacity-0 group-hover:opacity-100 transition-opacity" onclick="copyCode(this)">
                    📋 Copy
                </button>
                <code class="text-sm text-gray-800 block">
                    mkdir my-mcp-server<br>
                    cd my-mcp-server<br>
                    npm init -y
                </code>
            </div>
        </div>

        <div class="bg-white shadow-md rounded-lg p-6 relative">
            <div class="step-number absolute -left-4 top-6">3</div>
            <h2 class="text-2xl font-semibold text-gray-700 mb-4">Install Dependencies 📦</h2>
            <div class="bg-gray-100 p-4 rounded-md mb-4 relative group">
                <button class="absolute right-2 top-2 text-gray-400 hover:text-blue-500 opacity-0 group-hover:opacity-100 transition-opacity" onclick="copyCode(this)">
                    📋 Copy
                </button>
                <code class="text-sm text-gray-800 block">
                    npm install @modelcontextprotocol/sdk zod
                </code>
            </div>
            <p class="text-gray-600 mt-4">
                <span class="text-blue-500 mr-2">ℹ️</span>
                This will install the MCP SDK and Zod for parameter validation.
            </p>
        </div>

        <div class="bg-white shadow-md rounded-lg p-6 relative last-step">
            <div class="step-number absolute -left-4 top-6">4</div>
            <h2 class="text-2xl font-semibold text-gray-700 mb-4">Update package.json ✏️</h2>
            <p class="text-gray-600 mb-4">Add the following to your package.json:</p>
            <div class="bg-gray-100 p-4 rounded-md relative group">
                <button class="absolute right-2 top-2 text-gray-400 hover:text-blue-500 opacity-0 group-hover:opacity-100 transition-opacity" onclick="copyCode(this)">
                    📋 Copy
                </button>
                <code class="text-sm text-gray-800 block">
                    {<br>
                    &nbsp;&nbsp;"type": "module"<br>
                    }
                </code>
            </div>
        </div>

        <div class="flex justify-between items-center mt-12">
            <a href="/" class="inline-flex items-center text-gray-500 hover:text-blue-500 transition duration-300">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                </svg>
                Previous: Home
            </a>
            <a href="/tutorial" class="inline-flex items-center bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600 transition duration-300">
                Next: Tutorial
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
            </a>
        </div>
    </div>
</div>

<!-- Copy to Clipboard Script -->
<script>
function copyCode(button) {
    const codeBlock = button.nextElementSibling;
    const text = codeBlock.innerText;
    
    navigator.clipboard.writeText(text).then(() => {
        const originalText = button.innerText;
        button.innerText = '✅ Copied!';
        setTimeout(() => {
            button.innerText = originalText;
        }, 2000);
    });
}
</script>
` }) %> 